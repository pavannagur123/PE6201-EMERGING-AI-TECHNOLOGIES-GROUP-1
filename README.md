# PE6201 A2 — Applied AI System (Problem A)

This repository contains a single-agent health-insurance claim first-response system, its fixture data, tool layer, guardrails, evaluation harness, live-model results, cost model and reproduced failures.

All implementation code is contained in one notebook: `A2_CODE.ipynb`.

## Notebook execution

Open `A2_CODE_201.ipynb` in Google Colab and mount Google Drive when requested.




## V2 Rerun Results and Selected Run for Calculations

V2 was rerun, and both runs are included in the repository. The report and cost calculations use the earlier run consistently. Differences between the saved results reflect variation across runs of the same V2 configuration, not a change in the version.

